# Eclipse Mosquitto configuration for PureProtX IoT testing
# Anonymous access enabled for development/testing

# Bind on all interfaces (needed for Mosquitto 2.0 non-local access)
listener 1883 0.0.0.0
allow_anonymous true

log_dest stdout
